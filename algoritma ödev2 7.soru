#include <stdio.h>

int main() {
    int a, b, c;
    a = 8;
    b = 15;
    c = a / b + b / a * 2;
    b = b / 2;
    a = a + 3;

    printf("a = %d\n", a);
    printf("b = %d\n", b);
    printf("c = %d\n", c);

    return 0;
}
